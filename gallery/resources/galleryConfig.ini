; This is the default UberGallery config file. Copy this file to galleryConfig.php
; and change the following values to customize your gallery.

[basic_settings]

    cache_expiration    = 20           ; Cache expiration time in minutes
                                        ; Set to -1 for permanent caching

    enable_pagination   = false         ; Set to 'true' to enable pagination

    paginator_threshold = 12            ; Maximum number of pages to display
                                        ; in the paginator before truncating

    thumbnail_width     = 180            ; Thumbnail width (in pixels)

    thumbnail_height    = 180            ; Thumbnail height (in pixels)

    thumbnail_quality   = 82            ; Thumbnail quality from 1 - 100
                                        ; Higher = better quality / slower

    theme_name          = uber-responsive     ; Theme used to style the gallery


[advanced_settings]

    images_per_page     = 16          ; Images displayed per page, requires
                                        ; enable_pagination be set to 'true'

    images_sort_by      = natcasesort   ; Method used to sort image array
                                        ; Available sorting options include:
                                        ; asort, arsort, ksort, krsort,
                                        ; natcasesort, natsort, shuffle

    reverse_sort        = false         ; Set to 'true' to reverse sort order

    enable_debugging    = true         ; Output debug messages

    sajat_debug   = true               ;   ha true, akkor kinyomtatja a saját debug üzeneteket
